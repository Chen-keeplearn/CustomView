<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.al.custom_view">

    <application
        android:name=".CustomViewApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".path_menu.PathMenuActivity" />
        <activity android:name=".loading_view.LoadingViewActivity" />
        <activity android:name=".rating_view.RatingViewActivity" />
        <activity android:name=".step_view.QQStepViewActivity" />
        <activity android:name=".loading58.Loading58ViewActivity" />
        <activity android:name=".text_view_color_track.ColorTrackActivity" />
        <activity android:name=".dashboard.DashboardActivity" />
        <activity android:name=".small_red_book_start.SmallRedBookStartActivity" />
        <activity android:name=".hen_coder_course.PracticeActivity" />
        <activity android:name=".drawing_board.DrawingBoardActivity" />
        <activity android:name=".circle_progress_bar.CircleProgressBarActivity" />
    </application>

</manifest>